install ollama/phi3.5
set virtual environment 
install virtual environment/dependencies 
python -m venv venv
venv\Scripts\activate
pip install fastapi uvicorn pandas openpyxl chromadb sentence-transformers requests
